import React from 'react';

// import components
import Navbar from './navbar';

const Settings = () => {

    return (
        <div>
            <Navbar/>
        </div>
    )
};

export default Settings;
